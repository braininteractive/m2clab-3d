{% extends('layout/base.twig') %}

{% block content %}
  <header>
    <div class="col-xs-12">
      <h1>Think it. Print it.</h1>
    </div>
    <div class="buttons">
      <a href="/login" class="btn btn__invert">Login</a>
      <a href="" class="btn btn__invert">Shop erstellen</a>
    </div>

  </header>
  <div class="row">
    {% for shop in shops %}
      <div class="subshop columns medium-5" style="background-image: url('{{ shop.image }}');">
        <a href="/shop/{{ shop.name }}" class="subshop--link" style="position: absolute; bottom: 20px; left: 20px;">
          <h2>{{ shop.name }}</h2><br>
          <span>Jetzt entdecken!</span>
        </a>

      </div>
    {% endfor %}
  </div>
{% endblock %}