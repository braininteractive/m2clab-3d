{% extends('layout/base.twig') %}

{% block content %}
    {% for shop in shops %}
        <div class="medium-4 grid-block">
            <h2>{{ shop.name }}</h2>
            <p>{{ shop.description }}</p>
        </div>
    {% endfor %}
{% endblock %}